// Root-level Vite config placeholder.
//
// Vercel auto-detects the framework by looking for a `vite.config.*` file at
// the repo root. The real frontend lives in `artifacts/screening-console/`
// (its own vite.config.ts drives the actual build via the `vercel-build`
// script), so this file only exists so Vercel classifies this project as a
// static Vite site rather than a Node.js server.
export default {};
